n, m = map(int, input().split())
arr=list(map(int, input().split()))

arr.sort() #오름차순 정렬
arrlen = len(arr)

# i보다 큰 arr의 개수만큼 count 해줌
cnt=0
for i in arr :
    for j in arr :
        if i>j:
            cnt+=1
print(cnt)
