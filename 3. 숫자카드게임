n, m = map(int, input().split
result=0 #카드숫자가 모두 양수이므로 result (max값) 0으로 초기화

#result와 배열의 최솟값을 비교해 큰 값을 result에 업데이트
for i in range(n):
    arr=list(map(int, input().split()))
    result = max(result, min(arr))

print(result)
